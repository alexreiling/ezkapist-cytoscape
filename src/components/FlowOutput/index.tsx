import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

type indexProps = {};

const index: React.FC<indexProps> = (props) => {
  return <Wrapper>Flow Output</Wrapper>;
};

export default index;
